Example device lists go here. A device list is a `<name>.dev` file plus a
`<name>/` folder with the per-device settings and command files — qmeas
creates and edits these through its dialogs (Devices & Commands -> New).
